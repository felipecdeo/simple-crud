import React from 'react'
import {Header, HeaderTitle} from "./Header.styles"

export default props => {
  return (
    <Header>
      <HeaderTitle> Lista de Clientes </HeaderTitle>
    </Header>
  )
}
